# SPEC.md — CSV基本統計・可視化ツール

## 概要
CSVファイルを読み込み、列ごとの基本統計量（平均・中央値・標準偏差・欠損値数）と数値列間の相関係数をコンソールに出力し、
日別の推移がわかる折れ線グラフを画像ファイルとして出力するCLIツール。

## 0. サンプルデータ

`sample_data.csv`（店舗の日次データ、20行）を用意した。ツールの動作確認・仕様検討はこのファイルを基準に行う。

```csv
date,sales,visitors,temperature,weather
2024-01-01,120000,85,5.2,晴れ
2024-01-02,98000,70,6.1,曇り
2024-01-03,,60,,雨
...
```

- `date`: 日付（X軸として折れ線グラフに使用）
- `sales`, `visitors`, `temperature`: 数値列（統計量・相関・折れ線の対象。各列に欠損値を数件含む）
- `weather`: 非数値列（件数・欠損値数のみ算出。統計量は `-`）

## 1. 入力

- **形式**: カンマ区切りCSV（1行目はヘッダー行）。
- **文字コード**: UTF-8（BOM付き `utf-8-sig` も許容）を想定。それ以外のエンコーディングは対象外（必要ならユーザーが変換）。
- **列の種類**:
  - 数値列（int/float）: 統計量の算出対象。
  - 数値に変換できない列（文字列・カテゴリ・日付文字列など）: 統計量は「欠損値数」「件数」のみ算出し、平均・中央値・標準偏差は `N/A` とする。
  - 日付/時刻らしい列（列名に `date`/`time` を含む、またはパース可能な場合）: 折れ線グラフの候補として扱う。
- **欠損値**: 空文字列、`NA`、`NaN`、`null` などを欠損として扱う（pandasのデフォルト挙動に準拠）。
- **サイズ**: 数千〜数十万行程度の小〜中規模CSVを想定（メモリに全読み込みする前提）。巨大ファイル（GB級）は非対応。

### 入力例
```csv
date,temperature,humidity,note
2024-01-01,5.2,60,
2024-01-02,6.1,58,晴れ
2024-01-03,,55,雨
```

## 2. 出力

### 2.1 コンソール出力（標準出力）
列ごとに以下を表形式で出力する。
- 列名
- 件数（non-null count）
- 欠損値数（missing count）
- 平均（mean）※数値列のみ
- 中央値（median）※数値列のみ
- 標準偏差（std）※数値列のみ

数値以外の列は平均・中央値・標準偏差を `-` 表示し、件数・欠損値数のみ表示する。

出力例:
```
列名           件数    欠損値数   平均      中央値    標準偏差
--------------------------------------------------------------
date           3       0          -         -         -
temperature    2       1          5.65      5.65      0.636
humidity       3       0          57.67     58.00     2.517
note           2       1          -         -         -
```

オプションで統計量をCSVファイルにも出力できるようにする（`--stats-out summary.csv`）。

### 2.2 相関係数（数値列間）
数値列が2つ以上ある場合、ピアソンの相関係数を算出し、コンソールに相関行列として出力する（`pandas.DataFrame.corr()`を使用）。
欠損値がある行はペアワイズで除外して計算する（pandasのデフォルト挙動）。

出力例（sample_data.csvの場合）:
```
相関係数:
              sales    visitors  temperature
sales         1.000    0.968     0.891
visitors      0.968    1.000     0.873
temperature   0.891    0.873     1.000
```

数値列が1つ以下の場合はこのセクションをスキップする。

### 2.3 グラフ出力（画像ファイル）
- **メイン**: 日付列をX軸、数値列（sales, visitors, temperatureなど）をY軸にとった日別の折れ線グラフを列ごとに1枚出力する。日付らしい列（列名に`date`/`time`を含む、またはパース可能）がない場合は行インデックスをX軸として代用する。
- **出力形式**: PNG画像。デフォルトの出力先は `./output/` ディレクトリ（存在しなければ自動作成）。
- **ファイル名規則**: `line_<列名>.png`（例: `line_sales.png`）。
- グラフを出さないオプション（`--no-plot`）も用意する。
- ヒストグラムや散布図は今回のスコープ外（将来的な拡張候補）。

## 3. 使用ライブラリ候補

| ライブラリ | 用途 | 採用理由 |
|---|---|---|
| **pandas** | CSV読み込み、欠損値処理、統計量計算（mean/median/std） | CSV I/Oと欠損値・型推論の扱いが標準的で堅牢。`describe()`等の集計APIが揃っており実装コストが低い。 |
| **matplotlib** | ヒストグラム・折れ線グラフの描画 | 追加の依存が少なく、PNG出力が簡単。pandasとの親和性が高い（`DataFrame.plot`経由でも利用可）。 |
| **argparse**（標準ライブラリ） | CLI引数のパース | 外部依存なしでコマンドラインオプション（入力パス、出力先、プロット有無など）を実装できる。 |
| （任意）**tabulate** | コンソール表の整形出力 | 見やすい表形式出力が簡単に作れるが、必須ではない（標準の文字列整形でも代替可）。 |

依存関係は最小限（pandas, matplotlib、任意でtabulate）とし、`requirements.txt` にまとめる想定。

## 4. 想定する使い方（コマンド例）

```bash
# 基本: CSVを読み込み、統計量・相関係数をコンソール出力し、./output/ に折れ線グラフを保存
python csv_stats.py sample_data.csv

# 出力先ディレクトリを指定
python csv_stats.py data.csv --output-dir results/

# グラフを作らず統計量だけ確認したい場合
python csv_stats.py data.csv --no-plot

# 統計量をCSVファイルにも保存
python csv_stats.py data.csv --stats-out summary.csv

# 特定の列だけを対象にする
python csv_stats.py data.csv --columns temperature,humidity
```

### 想定オプション一覧
| オプション | 説明 | デフォルト |
|---|---|---|
| `input` (位置引数) | 入力CSVファイルパス | 必須 |
| `--output-dir` | グラフ画像の出力先ディレクトリ | `./output` |
| `--stats-out` | 統計量を書き出すCSVパス（省略時は書き出さない） | なし |
| `--no-plot` | グラフを生成しない | false |
| `--columns` | 対象列をカンマ区切りで指定（省略時は全列） | 全列 |

## 5. スコープ外（今回は対応しない）
- 複数CSVファイルの結合・比較
- 対話的なダッシュボード（Webアプリ化）
- 外れ値検出・異常検知などの高度な分析
- 非UTF-8エンコーディングの自動判定
- ヒストグラム・散布図（相関の可視化は数値の相関行列出力にとどめる）
- 欠損値の補完（欠損値数の報告のみ行い、値の補完・除外前提の分析は行わない）
